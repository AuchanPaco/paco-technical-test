spring:
  application:
    name: technical-test-api
  main:
    web-application-type: reactive
  data:
    mongodb:
      database: auchan-tech-test

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html
